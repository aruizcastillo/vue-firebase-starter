rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Auth-only mode has no starter-owned Firestore data model. Keep the
    // database default-deny until a derived project adds explicit rules for
    // its business collections.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
